{
  "name": "feedbase",
  "scripts": {
    "build": "DEBUG=feedbase:* yarn run hardhat compile",
    "test": "DEBUG=feedbase:* yarn build && mocha test"
  },
  "main": "index.js",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "chai": "^4.3.4",
    "hardhat": "^2.4.3",
    "ipfs-core": "^0.9.0"
  },
  "dependencies": {
    "ethers-eip712": "^0.2.0"
  }
}
